{
  "name": "dotfiles",
  "version": "2.0.0",
  "description": "TypeScript-based dotfiles management CLI",
  "type": "module",
  "scripts": {
    "build": "bun run build:mac && bun run build:ubuntu",
    "build:mac": "mkdir -p dist/mac dist/mac-arm && bun build --compile --minify src/cli.ts --target macos-x64 --outfile dist/mac/dotfiles && bun build --compile --minify src/cli.ts --target macos-arm64 --outfile dist/mac-arm/dotfiles",
    "build:ubuntu": "mkdir -p dist/ubuntu dist/ubuntu-arm && bun build --compile --minify src/cli.ts --target linux-x64 --outfile dist/ubuntu/dotfiles && bun build --compile --minify src/cli.ts --target linux-arm64 --outfile dist/ubuntu-arm/dotfiles",
    "build:local": "mkdir -p dist && bun build --compile --minify src/cli.ts --target bun --outfile dist/dotfiles",
    "dev": "bun run src/cli.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "zod": "^3.24.1",
    "chalk": "^5.3.0",
    "inquirer": "^12.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/inquirer": "^9.0.7",
    "typescript": "^5.7.2"
  },
  "bin": {
    "dotfiles": "./dist/dotfiles"
  }
}

